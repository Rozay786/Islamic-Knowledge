# SMTP configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM="Website Contact <no-reply@example.com>"

# The email that should receive questions
CONTACT_RECEIVER_EMAIL=your.email@example.com

# App
PORT=3000
